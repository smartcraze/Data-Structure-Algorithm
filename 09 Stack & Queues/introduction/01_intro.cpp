#include <bits/stdc++.h>
using namespace std;

int main() {
    stack<int>mystack;
    mystack.pop();
    mystack.push(1);
    mystack.push(2);
    mystack.push(3);
    mystack.top();
    mystack.size();

    // cout << mystack;

    return 0;
}